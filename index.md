# DocuSift — Privacy policy

Ultimo aggiornamento / Last updated: 2026-07-05
Contatto / Contact: polinix73@gmail.com

## Italiano

DocuSift funziona localmente sul dispositivo. Non richiede account, non invia documenti a server esterni e non usa servizi cloud per l'analisi. Anche la classificazione AI avviene interamente sul PC tramite un modello locale: nessun contenuto dei documenti lascia il dispositivo.

Dati trattati localmente:
- percorsi file selezionati dall'utente;
- metadati di analisi;
- categoria, data, nome suggerito e cartella proposta;
- hash file;
- testo estratto solo se l'utente abilita l'opzione relativa.

Acquisti: l'eventuale acquisto della versione Pro avviene tramite Microsoft Store ed e' gestito da Microsoft secondo la sua informativa privacy. L'app verifica soltanto lo stato della licenza tramite le API dello Store; nessun dato dei documenti e' coinvolto.

L'utente puo' cancellare l'indice locale e la cronologia dall'app con `Cancella tutti i dati locali`. Le operazioni sui file vengono applicate solo dopo conferma. Il comportamento predefinito non modifica mai gli originali.

## English

DocuSift runs locally on the device. It does not require an account, does not upload documents to external servers, and does not use cloud services for analysis. AI classification also happens entirely on the PC through a local model: no document content ever leaves the device.

Local data:
- file paths selected by the user;
- analysis metadata;
- category, date, suggested name, and suggested folder;
- file hash;
- extracted text only when the user enables that option.

Purchases: the optional Pro upgrade is handled by the Microsoft Store under Microsoft's privacy statement. The app only checks the license state through Store APIs; no document data is involved.

The user can clear the local index and history from the app with `Clear all local data`. File operations are applied only after confirmation. By default, originals are never modified.

## Francais

DocuSift fonctionne localement sur l'appareil. Aucun compte n'est requis, aucun document n'est envoye a des serveurs externes et aucun service cloud n'est utilise pour l'analyse. La classification IA s'effectue aussi entierement sur le PC via un modele local : aucun contenu de document ne quitte l'appareil.

Donnees locales:
- chemins des fichiers choisis par l'utilisateur;
- metadonnees d'analyse;
- categorie, date, nom suggere et dossier propose;
- hash du fichier;
- texte extrait uniquement si l'utilisateur active cette option.

Achats : l'eventuelle mise a niveau Pro est geree par le Microsoft Store selon la politique de confidentialite de Microsoft. L'application verifie uniquement l'etat de la licence via les API du Store ; aucune donnee de document n'est concernee.

L'utilisateur peut effacer l'index local et l'historique depuis l'application avec `Effacer toutes les donnees locales`. Les operations sur les fichiers ne sont appliquees qu'apres confirmation. Par defaut, les originaux ne sont jamais modifies.

## Espanol

DocuSift funciona localmente en el dispositivo. No requiere cuenta, no sube documentos a servidores externos y no usa servicios en la nube para el analisis. La clasificacion con IA tambien ocurre completamente en el PC mediante un modelo local: ningun contenido de los documentos sale del dispositivo.

Datos locales:
- rutas de archivos seleccionadas por el usuario;
- metadatos de analisis;
- categoria, fecha, nombre sugerido y carpeta propuesta;
- hash del archivo;
- texto extraido solo si el usuario activa esa opcion.

Compras: la posible actualizacion a Pro la gestiona Microsoft Store segun la politica de privacidad de Microsoft. La app solo comprueba el estado de la licencia mediante las API de la Store; no interviene ningun dato de los documentos.

El usuario puede borrar el indice local y el historial desde la app con `Borrar todos los datos locales`. Las operaciones sobre archivos solo se aplican tras confirmacion. De forma predeterminada, los originales nunca se modifican.

## Deutsch

DocuSift laeuft lokal auf dem Geraet. Es ist kein Konto erforderlich, Dokumente werden nicht an externe Server hochgeladen, und fuer die Analyse werden keine Cloud-Dienste verwendet. Auch die KI-Klassifizierung erfolgt vollstaendig auf dem PC ueber ein lokales Modell: Kein Dokumentinhalt verlaesst das Geraet.

Lokale Daten:
- vom Benutzer ausgewaehlte Dateipfade;
- Analysemetadaten;
- Kategorie, Datum, vorgeschlagener Name und vorgeschlagener Ordner;
- Datei-Hash;
- extrahierter Text nur, wenn der Benutzer diese Option aktiviert.

Kaeufe: das optionale Pro-Upgrade wird ueber den Microsoft Store gemaess der Datenschutzerklaerung von Microsoft abgewickelt. Die App prueft nur den Lizenzstatus ueber Store-APIs; Dokumentdaten sind nicht beteiligt.

Der Benutzer kann lokalen Index und Verlauf in der App mit `Alle lokalen Daten loeschen` entfernen. Dateioperationen werden nur nach Bestaetigung angewendet. Standardmaessig werden Originale nie geaendert.
